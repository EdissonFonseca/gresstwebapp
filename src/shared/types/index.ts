/**
 * Shared TypeScript types used across features.
 * Feature-specific types live in the feature's types/ folder.
 */

// Placeholder: add shared types as needed
// export type { Id } from './common';
