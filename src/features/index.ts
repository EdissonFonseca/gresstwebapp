/**
 * Features — one entry per user-visible page or self-contained functionality.
 * Each feature exposes its public API via index.ts.
 */

export * from './auth';
export * from './home';
export * from './user-profile';
