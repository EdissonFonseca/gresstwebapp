/**
 * Home feature — public API.
 * Maps to the "/" (home) page. One user-visible page = one feature.
 */

export { HomePage } from './components';
export { useHome } from './hooks';
export type { HomePageProps } from './types';
