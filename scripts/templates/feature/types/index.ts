/**
 * __FEATURE_TITLE__ feature types.
 */

// Add feature-specific types and interfaces here.
// For page-like features: PageProps, domain types, and state unions for hooks.

export interface __FEATURE_PASCAL__PageProps {
  // Presentational props: data and callbacks from the feature hook.
}
