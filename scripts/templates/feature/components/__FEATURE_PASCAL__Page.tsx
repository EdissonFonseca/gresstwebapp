import type { __FEATURE_PASCAL__PageProps } from '../types';

/**
 * __FEATURE_TITLE__ page. Presentational only; data and callbacks via props.
 */
export function __FEATURE_PASCAL__Page(_props: __FEATURE_PASCAL__PageProps) {
  return (
    <div className="__FEATURE_NAME__-page">
      <h1>__FEATURE_TITLE__</h1>
      <p>Feature content — wire props from use__FEATURE_PASCAL__().</p>
    </div>
  );
}
