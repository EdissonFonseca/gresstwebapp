# API base URL (no trailing slash). All requests are browser-side; backend is decoupled.
# Examples: https://qa.api.gresst.com (QA), https://api.gresst.com (production)
VITE_API_BASE_URL=https://api.example.com

# Set to "true" to send cookies with requests (e.g. when backend uses httpOnly cookie for auth).
# VITE_API_USE_CREDENTIALS=false

# Dev only: Bearer token for local testing when the server normally uses HttpOnly cookie.
# Get a token from Swagger/Postman (e.g. https://qa.api.gresst.com) and paste here (no "Bearer " prefix needed).
# Do not commit .env; this is for local QA testing only.
# VITE_DEV_BEARER_TOKEN=

# When the API blocks localhost (CORS), proxy /api to the API so the browser only talks to localhost.
# Set VITE_API_BASE_URL= (empty) and uncomment:
# VITE_PROXY_API_TARGET=https://qa.api.gresst.com
